{
  "rules": {
    "names": {
      ".read": "true",
      ".write": "false"
    },
    "1234": {
      ".read": "true",
      ".write": "true",
      "$post": {
        ".validate": "newData.hasChildren(['voltage', 'temperature', 'signal']) && newData.child('signal').isNumber() && newData.child('voltage').isNumber() && newData.child('temperature').isNumber()"
      }
    },
    "2854273": {
      ".read": "true",
      ".write": "true",
      "$post": {
        ".validate": "newData.hasChildren(['voltage', 'temperature', 'signal']) && newData.child('signal').isNumber() && newData.child('voltage').isNumber() && newData.child('temperature').isNumber()"
      }
    }
  }
}